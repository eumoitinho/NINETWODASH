import LoginForm from "@/components/auth/LoginForm";

export const metadata = {
  title: "Login - NINETWODASH | Dashboard de Marketing Digital",
  description: "Acesse seu dashboard do NINETWODASH para acompanhar métricas de Google Ads, Facebook Ads e Google Analytics em tempo real.",
};

const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;